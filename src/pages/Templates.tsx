import { Container, Text } from "tempest-ui";

const Templates: React.FC = () => {
  return (
    <Container>
      <Text variant="extra-large" color="primary">
        Templates
      </Text>
      <Text>Go ahead and take them, they're on the house.</Text>
    </Container>
  );
};

export default Templates;
